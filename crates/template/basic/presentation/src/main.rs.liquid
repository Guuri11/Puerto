mod api;
mod generated;

use poem::{Server, listener::TcpListener};

#[tokio::main]
async fn main() -> Result<(), std::io::Error> {
    Server::new(TcpListener::bind("0.0.0.0:8080"))
        .run(generated::bootstrap::build_app())
        .await
}
