[package]
name = "business"
version = "0.1.0"
edition = "2021"

[dependencies]
async-trait = "0.1"
thiserror = "2"
mockall = { version = "0.13", optional = true }

[features]
test-utils = ["mockall"]

[dev-dependencies]
tokio = { version = "1", features = ["rt", "macros"] }
mockall = "0.13"
